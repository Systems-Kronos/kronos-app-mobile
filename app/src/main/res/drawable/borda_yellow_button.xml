<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fundo transparente -->
    <solid android:color="@android:color/transparent" />

    <!-- Borda amarela -->
    <stroke
        android:width="3dp"
        android:color="#EEBE2F" /> <!-- Amarelo ouro -->

    <!-- Cantos arredondados -->
    <corners android:radius="12dp" />
</shape>